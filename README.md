# tictactoe
Game TicTacToe

```
new gameTicTacToe({
      container: 'body', // id, class or html element
      width: '500px', // px 
      height: '500px', // px 
      boardColor: '#000000',
      cellColor: '#ffffff',
      wheelColor: '#107d1d',
      crossColor: '#db000e',
});
```
